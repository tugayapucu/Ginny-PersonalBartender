{
  "name": "personalbartender",
  "private": true,
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm --prefix client run dev\"",
    "dev:backend": "npm run migrate && python -m uvicorn main:app --reload --app-dir backend",
    "dev:client": "npm --prefix client run dev",
    "migrate": "python -m alembic -c backend/alembic.ini upgrade head"
  }
}
